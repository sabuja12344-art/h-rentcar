# H-RENT CAR — 장기렌트 홈페이지 + 어드민 제작 지시서

> 이 문서를 Claude Code 프로젝트 루트에 `PROJECT.md` 로 저장하고,
> 첫 세션에서 **"PROJECT.md를 읽고 Step 0부터 순서대로 진행해줘"** 라고 지시하세요.
> 각 Step이 끝날 때마다 반드시 멈추고 사람이 확인한 뒤 다음 Step으로 넘어갑니다.

---

## 0. 프로젝트 개요

광주 지역 **월 장기렌트** 업체의 홈페이지와 관리자(어드민) 시스템을 만든다.

구성은 세 부분이다.

1. **방문자용 홈페이지** — 차량 소개 + 상담 신청
2. **관리자 페이지(어드민)** — 차량 등록/수정/삭제, 상담 내역 확인
3. **상담 DB** — 고객 문의 저장, 어드민에서 열람

가장 중요한 목표: **안정성**. 이전 프로젝트에서 모바일 깨짐, 버튼 미작동, 폼 미전송 오류가 있었다. 이번에는 그런 일이 없도록 아래 규칙을 엄격히 지킨다.

---

## 1. 절대 규칙 (반드시 지킬 것)

- **한 번에 하나의 Step만** 진행한다. Step이 끝나면 무엇을 했는지 요약하고 멈춘다. 다음 Step은 사람이 "진행"이라고 해야 시작한다.
- **큰 파일을 한꺼번에 생성하지 않는다.** 기능 단위로 작은 파일을 만들고, 각 파일 작성 후 동작을 확인한다.
- 새 라이브러리를 추가할 때는 **이유를 먼저 설명**하고 승인받는다. 불필요한 의존성은 넣지 않는다.
- 모든 기능은 **모바일(390px)과 PC(1280px) 둘 다에서 작동**해야 한다. 각 화면 완성 시 두 뷰포트 모두 확인한다.
- 외부 링크(전화/문자/카카오/인스타)는 **실제 값을 하드코딩하지 말고**, 설정 파일 한 곳에서 관리한다. 값은 임시 플레이스홀더로 두고 나중에 교체 가능하게 한다.
- 에러가 나면 임의로 우회하지 말고, **원인을 설명한 뒤** 해결 방법을 제시하고 진행 여부를 묻는다.
- 커밋은 Step 단위로 의미 있게 나눈다. (git 사용)

---

## 2. 기술 스택

과하지 않게, 유지보수와 배포가 쉬운 구성으로 한다.

- **프레임워크**: Next.js (App Router) + TypeScript
- **스타일**: Tailwind CSS (레퍼런스 시안의 디자인 토큰을 그대로 반영)
- **데이터베이스**: 초기에는 SQLite + Prisma ORM (파일 기반, 셋업 간단). 추후 PostgreSQL로 이전 가능하도록 Prisma 스키마로 추상화.
- **인증(어드민)**: 간단한 세션 기반 로그인 (아이디/비밀번호 1계정으로 시작, 환경변수로 관리)
- **폼/유효성 검사**: 서버 액션 + zod 로 서버·클라이언트 양쪽 검증
- **배포 대상**: Vercel 우선 고려 (Next.js 기본 호환). DB는 배포 환경에 맞춰 Step 6에서 결정.

> 위 스택이 부적절하다고 판단되면, 이유와 대안을 제시하고 사람의 승인을 받은 뒤 변경한다.

---

## 3. 디자인 토큰 (시안 기준 — 그대로 반영)

레퍼런스: 다크 톤(밤하늘 남색/검정) + 골드 액센트. "프리미엄 장기렌트" 느낌.

```
색상
  --bg-deep    #070b13   (최하단 배경)
  --bg         #0b111d
  --panel      #111a2b   (카드)
  --panel-2    #0f1727
  --line       rgba(255,255,255,.08)
  --line-strong rgba(255,255,255,.16)
  --ink        #f4f7fb   (기본 텍스트)
  --ink-soft   #aeb8c8
  --ink-dim    #6f7a8d
  --gold       #c8a15a   (핵심 액센트)
  --gold-soft  #e0c489

타이포
  본문/제목: Pretendard (한글 웹표준)
  숫자·가격 강조: 굵게(800~900)

레이아웃
  최대폭 1200px, 좌우 패딩 24px
  카드 radius 16px, 버튼 radius 12px
  섹션 상하 패딩: PC 88px / 모바일 60px
```

시안 HTML(`rentcar_main.html`)이 함께 전달된다. **이 파일을 시각적 기준으로 삼되**, 정적 HTML을 그대로 붙여넣지 말고 Next.js 컴포넌트 구조로 재구성한다.

---

## 4. 사이트 구조 (IA)

### 방문자 페이지
- `/` 메인 — 히어로(배너, 대표 차량 이미지) → 인기 차량 → 조건별 카테고리 → 상담(전화+폼) → 푸터
- `/fleet` 차량안내 — 전체 차량 목록 + 카테고리 필터
- `/fleet/[id]` 차량 상세 — 차량 정보 + 상담 신청 유도
- `/solutions` 장기렌트 안내 — 이용 방법, 요금 안내 (분량 적으면 메인 섹션으로 합칠 수 있음)
- 상담문의는 별도 페이지 대신 각 페이지 하단 폼 + 플로팅 버튼으로 처리

상단 메뉴: **차량안내 / 장기렌트 안내 / 상담문의** (3개만)

### 어드민 페이지
- `/admin/login` 로그인
- `/admin` 대시보드 — 신규 상담 건수, 차량 수 요약
- `/admin/cars` 차량 목록 (등록/수정/삭제)
- `/admin/cars/new`, `/admin/cars/[id]/edit` 차량 등록·수정 폼
- `/admin/inquiries` 상담 내역 (상태: 신규 → 상담중 → 완료)

---

## 5. 데이터 모델 (Prisma 스키마 기준)

### Car (차량)
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| id | string | ✓ | 자동 생성 |
| name | string | ✓ | 차량명 (예: 그랜저 IG 2.5) |
| category | enum | ✓ | 세단 / SUV / 승합·미니밴 / 경차 / 전기·친환경 |
| brand | string |  | 제조사 |
| thumbnail | string | ✓ | 대표 이미지 URL |
| images | string[] |  | 추가 이미지 |
| monthlyPrice | int | ✓ | 월 렌트료(만원 단위) |
| deposit | string |  | 보증금/선납 안내 |
| contractTerms | string |  | 계약기간 (예: 12/24/36/48개월) |
| mileage | string |  | 약정 주행거리 |
| year | string |  | 연식 |
| fuel | string |  | 연료 |
| seats | int |  | 인승 |
| options | string |  | 주요 옵션 |
| description | string |  | 상세 설명 |
| label | enum |  | 인기 / 신차 / 특가 / 즉시출고 / 없음 |
| sortOrder | int |  | 노출 순서 |
| isActive | boolean | ✓ | 노출 여부 |
| createdAt | datetime | ✓ | |

### Inquiry (상담)
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| id | string | ✓ | |
| name | string | ✓ | 이름 |
| phone | string | ✓ | 연락처 |
| carInterest | string |  | 희망 차종 |
| type | enum |  | 신규 / 견적 / 기타 |
| message | string |  | 문의 내용 |
| status | enum | ✓ | NEW / IN_PROGRESS / DONE (기본 NEW) |
| agreedPrivacy | boolean | ✓ | 개인정보 수집 동의 |
| createdAt | datetime | ✓ | |

> 카테고리/라벨 enum 값은 확정 전이므로, **한 곳(상수 파일)에서 관리**하여 나중에 쉽게 바꿀 수 있게 한다.

---

## 6. 연동 기능 (설정 파일로 관리)

`config/site.ts` 같은 단일 설정 파일에 아래 값을 모아둔다. 지금은 플레이스홀더, 나중에 실제 값으로 교체.

```
phone:        "1544-0000"        → tel: 링크
sms:          "1544-0000"        → sms: 링크
kakaoOpenUrl: "https://open.kakao.com/..."   (오픈채팅)
instagramUrl: "https://instagram.com/..."
naverUrl:     ""  (선택)
businessInfo: { name, regNo, licenseNo, address }
```

- 전화/문자 버튼 → `tel:`, `sms:` 링크 (모바일에서 바로 실행 확인)
- 카카오·인스타 → 새 탭으로 링크
- 우측 하단 플로팅 버튼: 전화 / 카카오 / 인스타 / 문자

---

## 7. 상담 접수 & 알림 (무료 방식으로 시작)

- 상담 폼 제출 → 서버 액션으로 **DB에 저장** → 어드민 `/admin/inquiries`에서 확인
- 폼 검증: 이름·연락처·개인정보 동의는 필수. 실패 시 어느 항목이 문제인지 명확히 표시.
- **알림(무료)**: 새 상담 저장 시 관리자 이메일로 알림 발송 (환경변수에 SMTP 또는 이메일 API 키). 키가 없으면 저장은 되고 알림만 스킵되도록 안전 처리.
- 유료 알림(카카오 알림톡/SMS)은 이번 범위에서 제외. 나중에 붙일 수 있도록 알림 발송 부분을 함수로 분리해 둔다.

---

## 8. Step별 진행 순서

각 Step 완료 후 **멈추고 확인받는다.**

- **Step 0 — 셋업**: Next.js + TS + Tailwind 프로젝트 생성, 디자인 토큰을 Tailwind 설정에 반영, Pretendard 적용, git init. 빈 페이지가 뜨는 것까지 확인.
- **Step 1 — 공통 레이아웃**: 헤더(반응형 메뉴/햄버거), 푸터, 플로팅 버튼, 사이트 설정 파일. PC/모바일 확인.
- **Step 2 — 메인 페이지**: 시안 기준으로 히어로(대표 이미지 영역 포함) → 인기 차량 → 카테고리 → 상담 섹션. 이 시점엔 차량 데이터는 임시 상수로.
- **Step 3 — DB & 차량 목록/상세**: Prisma 스키마, 시드 데이터, `/fleet` 목록+필터, `/fleet/[id]` 상세를 실제 DB에서 렌더링.
- **Step 4 — 상담 폼 + 저장**: 서버 액션으로 상담 저장, zod 검증, 성공/실패 UI. 이메일 알림(무료) 연결.
- **Step 5 — 어드민**: 로그인, 차량 CRUD, 상담 내역/상태 관리. 이미지 업로드 방식 결정.
- **Step 6 — 마무리 & 배포**: 반응형/접근성/에러 상태 점검, SEO 기본(title/OG), 배포 및 DB 운영 방식 확정.

---

## 9. 각 Step 완료 시 자가 점검 (매번 확인)

- [ ] 모바일(390px)·PC(1280px) 둘 다 레이아웃 정상인가
- [ ] 모든 버튼/링크가 실제로 눌리고 동작하는가
- [ ] 폼이 있다면 잘못된 입력에서 명확한 에러가 뜨는가
- [ ] 콘솔에 에러/경고가 없는가
- [ ] 키보드 포커스가 보이고, 이미지에 alt가 있는가
- [ ] 빌드(`next build`)가 통과하는가

---

## 10. 지금 미확정 항목 (진행 중 물어볼 것)

아래는 아직 안 정해졌다. 필요한 Step에서 **사람에게 질문**한 뒤 진행한다.

- 실제 전화/문자 번호, 카카오·인스타 링크, 사업자 정보
- 차량 카테고리 최종 분류 및 라벨 종류
- 차량 이미지(대표 사진) 확보 여부 / 업로드 방식
- 상담 알림 받을 이메일 주소
- 최종 배포 환경(Vercel 등)과 운영 DB
